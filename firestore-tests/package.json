{
  "name": "gamoitsani-firestore-tests",
  "private": true,
  "type": "module",
  "description": "Behavioural tests for firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "./run.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
